# sample_data/

Folder na  audio z datasetu `PolyAI/minds14` (pl-PL).

## Pobieranie próbek

```bash
pip install datasets soundfile
python scripts/download_sample.py
```

Skrypt pobiera nagrania i zapisuje je jako `.wav`.
