apiVersion: v1
kind: Service
metadata:
  name: chromadb-service
  namespace: audio-rag
  labels:
    app: chromadb
spec:
  selector:
    app: chromadb
  ports:
    - name:       http
      protocol:   TCP
      port:       8000
      targetPort: 8000
  type: ClusterIP
